name: authentication_repository
description: Dart package which manages the authentication domain.
version: 1.0.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  firebase_core: "^1.0.1"
  firebase_auth: "^1.0.1"
  equatable: ^2.0.0
